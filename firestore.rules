rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow full access for development. 
      // In production, restrict this using auth!
      allow read, write: if true;
    }
  }
}
